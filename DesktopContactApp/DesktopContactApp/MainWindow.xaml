<Window x:Class="DesktopContactApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DesktopContactApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    
    <StackPanel Margin="10">
        <Button Content="New Contact" 
                Click="NewContactButton_Click"/>

        <TextBox Margin="0,5,0,5"
                 TextChanged="SearchTextBox_TextChanged"/>

        <ListView x:Name="ContactsListView"
                  SelectionChanged="ContactsListView_SelectionChanged"
                  Height="280"
                  Margin="0,5,0,0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <TextBlock Text="{Binding Name}" FontSize="20" FontWeight="Bold" Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding Email}" FontSize="15" Foreground="DodgerBlue" Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding Phone}" FontSize="15" FontStyle="Italic" Margin="0,0,10,0"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</Window>